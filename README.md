# frame-work-smoke

A deliberately tiny public repo used as the work target for Papercusp's
cloud-frame "work-on-frame" verification (cloud-deployment-layer P-013
extended step). A deployed frame's own orchestrator loop completes small
work items against this repo. No secrets, nothing real.
